apply plugin: 'com.android.library'

android {
    compileSdkVersion safeExtGet("compileSdkVersion", 33)
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation(name: 'rscompanion_v.1.0.2', ext: 'aar')
}
